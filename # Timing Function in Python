# Timing Function in Python

import time

def measure_execution_time(func):
    start_time = time.time()
    
    # Call the provided function
    func()
    
    stop_time = time.time()
    duration = (stop_time - start_time) * 1000  # Convert to milliseconds

    return duration
